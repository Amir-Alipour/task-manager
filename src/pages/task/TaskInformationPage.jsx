import { useParams } from "react-router-dom"

function TaskInformationPage() {
    const {id} = useParams();


    return (
        <div className="container p-3 border-l border-r" style={{height: "94.3vh"}}>
            
        </div>
    )
}

export default TaskInformationPage
